<UserControl
    x:Class="Jg.wpf.app.Controls.ScrollViewerAnimationDemo"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:assist="clr-namespace:Jg.wpf.controls.Assist;assembly=Jg.wpf.controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="clr-namespace:Jg.wpf.app.ViewModels"
    d:DataContext="{d:DesignInstance viewModels:ScrollViewerAnimationViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <ScrollViewer
            Margin="0,15,0,0"
            assist:ScrollViewerHelper.AnimationDuration="0:0:0.5"
            assist:ScrollViewerHelper.AnimationEase="CubicOut"
            assist:ScrollViewerHelper.MouseWheelDelta="96"
            assist:ScrollViewerHelper.ScrollBarPosition="Outside"
            assist:ScrollViewerHelper.ScrollBarThickness="16"
            assist:ScrollViewerHelper.WheelScrollingDirection="Vertical"
            HorizontalScrollBarVisibility="Disabled"
            Style="{StaticResource Jg.AnimationScrollViewer}"
            VerticalScrollBarVisibility="Auto">

            <ItemsControl
                HorizontalAlignment="Stretch"
                VerticalContentAlignment="Stretch"
                ItemsSource="{Binding Source}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="1" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock FontSize="50" Text="{Binding .}" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <ScrollViewer
            Grid.Column="1"
            Margin="0,15,0,0"
            assist:ScrollViewerHelper.AnimationDuration="0:0:0.5"
            assist:ScrollViewerHelper.AnimationEase="CubicOut"
            assist:ScrollViewerHelper.MouseWheelDelta="96"
            assist:ScrollViewerHelper.ScrollBarPosition="Outside"
            assist:ScrollViewerHelper.ScrollBarThickness="16"
            assist:ScrollViewerHelper.WheelScrollingDirection="Horizontal"
            HorizontalScrollBarVisibility="Auto"
            Style="{StaticResource Jg.AnimationScrollViewer}"
            VerticalScrollBarVisibility="Disabled">

            <ItemsControl
                HorizontalAlignment="Stretch"
                VerticalContentAlignment="Stretch"
                ItemsSource="{Binding Source}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Rows="3" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock FontSize="50" Text="{Binding .}" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
