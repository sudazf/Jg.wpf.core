<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:numericTextBox="clr-namespace:Jg.wpf.controls.Customer.NumericTextBox">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.TextBox.xaml" />

    </ResourceDictionary.MergedDictionaries>

    <Style
        x:Key="Numeric.TextBox.Style"
        BasedOn="{StaticResource MaterialDesignTextBox}"
        TargetType="{x:Type numericTextBox:NumericTextBox}">
        <Setter Property="InputMethod.IsInputMethodEnabled" Value="False" />
        <Setter Property="materialDesign:TextFieldAssist.SuffixText" Value="{Binding Unit, RelativeSource={RelativeSource Mode=Self}}" />
    </Style>

    <Style
        x:Key="Normal.TextBox.Style"
        BasedOn="{StaticResource MaterialDesignTextBox}"
        TargetType="{x:Type TextBox}">
    </Style>

</ResourceDictionary>